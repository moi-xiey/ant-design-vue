import AMessage from 'ant-design-vue/es/message';
import './style.less';

AMessage.config({
  // top/duration/prefixCls/getContainer/transitionName/maxCount
  maxCount: 8,
});

export default AMessage;
